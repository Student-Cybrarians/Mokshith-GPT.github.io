export type Capability='reasoning'|'vision'|'text'; export interface AIRequest { capability:Capability; prompt:string; input?:unknown; correlationId?:string; maxTokens?:number; } export interface AIResponse { text:string; model:string; usage?:{units:number}; } export interface AIProvider { generate(req:AIRequest):Promise<AIResponse>; analyzeText(req:AIRequest):Promise<AIResponse>; analyzeVision(req:AIRequest):Promise<AIResponse>; healthCheck():Promise<boolean>; }
